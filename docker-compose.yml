version: "3.9"

services:

  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

  api:
    build: ./api
    restart: unless-stopped
    ports:
      - "3000:3000"
    depends_on:
      - mongo

volumes:
  mongo_data:
